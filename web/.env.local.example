# Copy to web/.env.local for local dev, and set the same two vars in Vercel.
# These use the ANON / publishable key (safe for the browser; RLS-protected).
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key-here
